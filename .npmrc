registry=https://registry.npmmirror.com
# electron 二进制走 npmmirror 镜像（@electron/get 经 npm_config_electron_mirror 读取），
# 避免打包时从 github 直连下载 electron-v*-win32-x64.zip（慢/超时）。
electron_mirror=https://npmmirror.com/mirrors/electron/